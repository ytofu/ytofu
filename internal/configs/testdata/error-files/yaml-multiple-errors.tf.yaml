# Test YAML error position accuracy with multiple errors
# This verifies each error points to its correct line

variable:
  first_var:
    type: string
    bad_var_attr: "error1"  # ERROR: Extraneous YAML property

output:
  first_output:
    value: "test"
    bad_output_attr: "error2"  # ERROR: Extraneous YAML property
