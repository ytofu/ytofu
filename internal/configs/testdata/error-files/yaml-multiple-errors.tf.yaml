# Test YAML error position accuracy with multiple errors
# This verifies each error points to its correct line

output:
  first_output:
    value: "test1"
    bad_output_attr1: "error1"  # ERROR: Extraneous YAML property

output:
  second_output:
    value: "test2"
    bad_output_attr2: "error2"  # ERROR: Extraneous YAML property
